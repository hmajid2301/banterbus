apiVersion: apps/v1
kind: Deployment
metadata:
  name: cloudflared-banterbus-dev
  labels:
    app: cloudflared-banterbus-dev
spec:
  replicas: 1  # Single replica for dev
  selector:
    matchLabels:
      app: cloudflared-banterbus-dev
  template:
    metadata:
      labels:
        app: cloudflared-banterbus-dev
    spec:
      containers:
      - name: cloudflared
        image: cloudflare/cloudflared:latest
        args:
        - tunnel
        - --no-autoupdate
        - run
        - --token
        - $(TUNNEL_TOKEN)
        env:
        - name: TUNNEL_TOKEN
          valueFrom:
            secretKeyRef:
              name: cloudflared-token  # Uses existing secret from k8s-gitops
              key: token
        livenessProbe:
          httpGet:
            path: /ready
            port: 2000
          failureThreshold: 1
          initialDelaySeconds: 10
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 2000
          failureThreshold: 1
          initialDelaySeconds: 10
          periodSeconds: 10
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi